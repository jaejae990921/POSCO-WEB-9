주 branch(main)에서 새로운 branch를 생성할 때,
습관처럼 git pull 사용하는게 좋음 => remote에 있는 최신상태를 내 local로 받아옴

git branch "새로운 branch 이름"
=> 브랜치 만들기

git checkout "새로운 branch 이름"
=> 브랜치 이동하기

개발

git checkout main

git merge "새로운 branch 이름"

// 내 로컬에서 합친 상태